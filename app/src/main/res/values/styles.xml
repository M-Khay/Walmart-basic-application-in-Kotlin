<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorButton</item>
        <item name="android:textViewStyle">@style/Walmart.Theme.TextView</item>
        <item name="android:buttonStyle">@style/Walmart.Theme.Button</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Walmart.Theme.TextView" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/bogle_family</item>
    </style>

    <style name="Walmart.Theme.Button" parent="android:Widget.Material.Button.Colored">
        <item name="android:fontFamily">@font/bogle_family</item>
    </style>

</resources>
